import React from 'react';
import { Box } from '@chakra-ui/react';

interface IProps {}

const CreationSelect = ({}: IProps) => <Box data-name="creation-select">content</Box>;

export default CreationSelect;
